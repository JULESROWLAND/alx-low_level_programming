#include <stdio.h>

/**
 * main - Entry point for all c file
 * Return: 0 Always (Success)
 */
int main(void)
{
	 char let[] = "abcdefghijklmnopqrstuvwxyz";
	 int i;

	for (i = 0; i <= 0; i++)
	{
		putchar(let[i]);
	}
	putchar('\n');
	return (0);
}
