#include <stdio.h>

/**
 * main - This ia an Entry Point
 * Return: This returns the value of 0.
 */
int main(void)
{
	int a;

	for (a = 0; a <= 9; a++)
	{
		printf("%d", a);
	}
	printf("\n");
	return (0);
}
