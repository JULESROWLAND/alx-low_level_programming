#include "main.h"

/**
 * print_alphabet - This is a function
 * main - Entry point
 * Return: 0 Always success
 */

void print_alphabet(void)
{
	char let;

	for (let = 'a'; let <= 'z'; let++)
	{
		_putchar(i);
	}
	_putchar('\n');
}
