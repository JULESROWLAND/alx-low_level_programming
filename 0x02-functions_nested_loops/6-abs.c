#include <stdlib.h>

/**
 * _abs - Check for absolute interger
 * Return: 0 always successful
 * @y: This creates data based on certain type
 */
int _abs(int y)
{
	if (y > 0)
	{
	return (y);
	}
	return (-y);
}
